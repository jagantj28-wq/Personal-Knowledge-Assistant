services:
  # ── Unified Full-Stack Docker Web Service ──
  - type: web
    name: personal-knowledge-assistant
    runtime: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: GEMINI_API_KEY
        sync: false
    healthCheckPath: /api/health
